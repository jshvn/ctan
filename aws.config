# Read by `task publish` via AWS_CONFIG_FILE. The CLI's default multipart threshold is
# 8 MB; the largest tlnet file is ~145 MB, so this keeps every upload one PutObject.
[default]
s3 =
    multipart_threshold = 200MB
    max_concurrent_requests = 32
